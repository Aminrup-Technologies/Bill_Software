# FLMX v2.1 Cross-Tenant Duplication — Enterprise Release

**Program status:** Released
**Release tag:** `v2.1-cross-tenant-enterprise`
**Merge commit:** `148e6ed52fdac692a299d5258b4bb58832f0e266` (PR #89)
**Promotion commit:** `18e76ab193c1a2b4c51b07fc21649ddaac1e62a3` (this milestone record)
**Integration PR:** [#89 — feat: cross-tenant duplication — vendor, customer & bulk operations (PR-1–4 integrated)](https://github.com/Aminrup-Technologies/Bill_Software/pull/89)
**UAT gate:** [`docs/45_CrossTenant_UAT_Checklist.md`](45_CrossTenant_UAT_Checklist.md)

This file is the release closeout record. Future duplication work references it and [`docs/41`](41_CrossTenant_Duplication_Architecture.md)–[`docs/48`](48_CrossTenant_Docs_Consistency_Audit.md); it does not replace the UAT checklist as the pre-production gate.

---

## Release Summary

Cross-tenant duplication for vendors and customers — single-item and bulk, with tenant guard rails, name-collision resolution, child-record cloning, and audit notifications aligned to the live `tbl_SystemNotification` schema — merged into `master` via merge commit `148e6ed` (two parents; append-only history preserved). The integration consolidated a four-PR stack, replayed the remediation work from a diverged sibling branch, reconciled against master's concurrently-landed impersonation runtime, and retired the legacy dual-baseline branch model.

## Merged Features

| Feature | Commit | Source PR |
|---|---|---|
| Duplication infrastructure (service, tenant guards, UI entry points) | `f50887e` | #85/#86 |
| Vendor duplication hardening (`VendorInCurrentCompany`) | `e59139a` | #86/#87 |
| Customer duplication + generalized helpers (`ClientInCurrentCompany`, child-record copy) | `a801195` | #87/#88 |
| Bulk duplication (`BulkDuplicateVendors` / `BulkDuplicateCustomers`, `BulkDuplicateResult`) | `cbf9790` | #88 |
| PR #85 remediation replay — UAT switcher/Switch User markup | `bdb288b` (replay of `199b349`) | #85 |
| PR #85 remediation replay — audit writes on live schema (`WriteDuplicationAudit`) | `5855899` (replay of `25b1a7f`) | #85 |
| Bulk-path audit conversion (closes the last dead-schema INSERTs) | `691f057` | pre-merge gate |
| Documentation corpus renumbered 34–40 → 41–48 | `05402be` | governance |
| Master reconciliation (impersonation runtime engine) | `f764f7e` | governance |

Final state on `master`: 5 audit call sites, 0 dead-schema (`Module`/`Type`/`UserId`) INSERTs, `SwitchUser.aspx(.cs)` byte-identical to the impersonation runtime baseline.

## Governance Changes

- **Single integration baseline:** `master` is the production trunk and the only integration baseline (`CONTRIBUTING.md` §1, updated `18e76ab`).
- **Default branch switched** from `July_to_Sept26_DevNSupport` to `master` — previously the reason the July branch could not be deleted.
- **Documentation-first numbering restored:** duplication corpus renumbered to 41–48; master's impersonation corpus retains 29–37; zero collisions.

## Repository Cleanup Completed

- Closed as superseded (work fully incorporated into #89): **#86, #87, #88**; **#85** closed after verified preservation (replay commits `bdb288b`, `5855899`); **#78** closed as superseded by master's merged impersonation corpus.
- Deleted branches: `July_to_Sept26_DevNSupport` (tip `fa777e9` is the direct parent of `148e6ed` — zero history loss) and `feat/cross-tenant-duplication-pr1/pr3/pr4` (tips ancestors of master).
- Recreated `feat/cross-tenant-duplication-pr2` (post-merge push by KK - TL containing the double-submit fix `c5d415e`) was **not deleted**: its unique fix is preserved as Draft **PR #90** on branch `fix/vendor-duplicate-double-submit`, extended to the identical customer-path defect.

## Validation Completed

- Pre-merge gates: mergeability CLEAN (GitHub API + local `merge-tree`), all 10 integration commits reachable, no doc-numbering collisions, single implementation path (no other open PR touched `DuplicationService`).
- Reconciliation validation: union resolution in `DuplicationService.cs` verified (remediation audit helper + stack validation helpers coexist); SwitchUser reconciliation verified against master's INV-13–17 runtime.
- Post-merge: master contains the full feature set; dead-schema audit INSERT count 0; closed PRs remain closed.

## Known Parked Work

- **PR #21** — `Restyle home.aspx dashboard onto the FLMX grid` — intentionally parked **Draft**, retargeted to `master`, MERGEABLE/CLEAN, head `c6c0198`.
- **PR #90** — `fix: defer duplicate-confirm button disable until postback starts (vendor + customer)` — Draft pending review/UAT. Merging it completes the double-submit defect closure for both duplication paths.
- UAT per [`docs/45`](45_CrossTenant_UAT_Checklist.md) remains the gate before enabling duplication in production tenants.

## Rollback Reference

- **Feature code:** revert the PR #89 merge commit — `DuplicationService.cs` is additive; no master file was destructively modified.
- **Audit fix:** `691f057` reverts independently (single file).
- **SQL:** `user_company_access_admin_aa_associates.sql` is idempotent permission grants — no schema migration to roll back.
- **Docs:** 41–48 and this record are additive; renumbering revertable without code impact.
- **Branch/PR metadata:** default-branch setting is reversible; deleted branches' commits remain reachable from master's history (no force-push occurred at any point).
